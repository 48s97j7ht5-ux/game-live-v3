# Copy to .env locally — never commit real keys.
MAGIC_PIXEL_API_KEY=
# From Magic Pixel dashboard / API docs (example placeholder):
MAGIC_PIXEL_API_BASE=https://api.example.com

# Leonardo.ai — from dashboard's "API Access" section (separate from the
# regular app subscription/credits on many plans, check there first).
LEONARDO_API_KEY=
