import{W,H,createCanvas}from'#pixel-app';
export default{ id:'io',mount(app){
  document.getElementById('spriteFile').onchange=e=>{const f=e.target.files[0];if(!f)return;const im=new Image();im.onload=()=>{app.history.pushStructure();const c=createCanvas();c.getContext('2d').drawImage(im,0,0,W,H);app.layers.importCanvas(c,'composite_import');URL.revokeObjectURL(im.src)};im.src=URL.createObjectURL(f)};
  document.getElementById('refFile').onchange=e=>{const f=e.target.files[0];if(!f)return;const im=new Image();im.onload=()=>{const ref=document.getElementById('ref'),ctx=ref.getContext('2d');ctx.clearRect(0,0,W,H);ctx.imageSmoothingEnabled=false;const s=Math.min(W/im.width,H/im.height),dw=Math.round(im.width*s),dh=Math.round(im.height*s);ctx.drawImage(im,Math.round((W-dw)/2),Math.round((H-dh)/2),dw,dh);URL.revokeObjectURL(im.src);app.emit('reference:changed')};im.src=URL.createObjectURL(f)};
  document.getElementById('export').onclick=()=>{app.compositor.render();const a=document.createElement('a');a.download='pixel-composite-135x400.png';a.href=app.compositor.canvas.toDataURL('image/png');a.click()};
  document.getElementById('exportLayer').onclick=()=>{const l=app.layers.active();if(!l)return;const a=document.createElement('a');a.download=l.name+'.png';a.href=l.canvas.toDataURL('image/png');a.click()};
}};