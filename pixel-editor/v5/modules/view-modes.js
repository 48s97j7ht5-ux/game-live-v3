export default{ id:'view-modes',mount(app){const box=document.getElementById('viewModes');const modes=[['composite','Composite'],['solo','Solo layer'],['underlay','Active + underlay']];for(const[id,label]of modes){const b=document.createElement('button');b.textContent=label;b.dataset.mode=id;b.onclick=()=>{app.state.viewMode=id;[...box.children].forEach(x=>x.classList.toggle('activeBtn',x.dataset.mode===id));app.emit('composite:dirty')};box.appendChild(b)}box.firstChild?.classList.add('activeBtn')} };